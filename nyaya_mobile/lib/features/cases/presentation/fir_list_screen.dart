import 'package:flutter/material.dart';

class FIRListScreen extends StatelessWidget {
  const FIRListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('FIRs'),
      ),
      body: const Center(
        child: Text('FIR List - Coming Soon'),
      ),
    );
  }
}
